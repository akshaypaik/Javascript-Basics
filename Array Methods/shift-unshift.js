let arr = [1,2,3,4,5];
arr.unshift(0); // returns length and adds an element to first
console.log(arr);
arr.shift(1);   // returns the removed element and removes an element from first
console.log(arr);

