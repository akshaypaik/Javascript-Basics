function calcAge(birthYear) {
  const age = 2023 - birthYear;
  console.log(firstName);
  //return `The age is ${age}.`;
  function printAge() {
    const statement = `The age is ${age}.`;
    if (age > 18) {
      var millenial = true;
    }
    console.log(millenial);
    return statement;
  }
  //console.log(millenial);
  return printAge();
}

const firstName = "Akshay";
console.log(calcAge(1995));
